---
// src/pages/now.astro
import LayoutDefault from "~/layouts/LayoutDefault.astro";


const AUTH_TOKEN = import.meta.env.NEODB_TOKEN;

if (!AUTH_TOKEN) {
  console.error("❌ 警告：未找到 NEODB_TOKEN 环境变量，NeoDB 数据将无法加载！");
}

// 2. 抓取数据的通用函数 (使用 api/me 接口，最稳！)
async function fetchNeoDB(category: string) {
  try {
    const res = await fetch(`https://neodb.social/api/me/shelf/fetch?category=${category}&limit=3`, {
      headers: {
        Authorization: `Bearer ${AUTH_TOKEN}`, 
        Accept: 'application/json',
      },
    });
    
    if (!res.ok) {
        console.error(`NeoDB fetch failed for ${category}: ${res.statusText}`);
        return [];
    }
    
    const json = await res.json();
    return json.data || [];
  } catch (error) {
    console.error(`NeoDB error for ${category}:`, error);
    return [];
  }
}

// 3. 在构建时直接抓取数据
// 因为用了 api/me 接口，系统会自动识别这个 Token 属于谁，不用填用户名
const recentMovies = await fetchNeoDB('movie');
const recentBooks = await fetchNeoDB('book');

// 自动获取构建日期
const lastModified = new Date().toISOString().split('T')[0];
---

<LayoutDefault >
  <article class="heti prose mx-auto">
    <h1>Now</h1>
    <p class="text-gray-500 italic">
      这是我的 <a href="/about" class="underline hover:text-blue-600">Now 页面</a>。最后更新于：{lastModified}
    </p>
    
    <h3>📍 当前状态</h3>
    <ul>
      <li><strong>坐标</strong> 加拿大</li>
      <li><strong>生活</strong> 永居到手，正在陆续办理各种文件和手续。</li>
      <li><strong>爱好</strong> 抱石 (Bouldering) & 克菲尔 (Kefir) 发酵。</li>
    </ul>

    {/* 电影板块 */}
    {recentMovies.length > 0 && (
      <>
        <h3>🎬 最近在看</h3>
        <div class="grid grid-cols-3 gap-4 my-4">
          {recentMovies.map((record: any) => (
            <a href={`https://neodb.social${record.item.url}`} target="_blank" class="flex flex-col items-center group normal">
              <div class="relative w-full aspect-[2/3] overflow-hidden rounded shadow-sm group-hover:shadow-md transition-all">
                <img 
                  src={record.item.cover_image_url} 
                  alt={record.item.title} 
                  class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105" 
                />
              </div>
              <span class="text-xs mt-2 text-center leading-tight text-gray-600 group-hover:text-black">
                {record.item.title}
              </span>
            </a>
          ))}
        </div>
      </>
    )}

    {/* 书籍板块 */}
    {recentBooks.length > 0 && (
      <>
        <h3>📚 最近在读</h3>
        <ul class="my-4 space-y-2">
          {recentBooks.map((record: any) => (
            <li class="flex items-center">
              <span class="mr-2">📖</span>
              <a href={`https://neodb.social${record.item.url}`} target="_blank" class="hover:underline text-gray-800">
                {record.item.title}
              </a>
              {/* 如果有评分就显示 */}
              {record.rating_grade && (
                <span class="text-xs text-gray-400 ml-2 border border-gray-200 px-1 rounded">
                  {record.rating_grade}/10
                </span>
              )}
            </li>
          ))}
        </ul>
      </>
    )}
    
    {/* 如果都没有数据，显示提示 */}
    {recentMovies.length === 0 && recentBooks.length === 0 && (
       <p class="text-gray-400 text-sm italic">暂时无法获取 NeoDB 数据，请稍后再试。</p>
    )}
    
  </article>
</LayoutDefault>